# TODO - Задачи на развитие

## 🔴 Критичные (нужно для production)

### Производительность
- [ ] **Streaming кластеризация** — сейчас весь файл читается в память при первом вызове. Для файлов >1GB нужен streaming подход с sampling
- [ ] **Инкрементальное обновление кэша** — при повторном анализе того же файла использовать предыдущие результаты
- [ ] **Ограничение памяти** — добавить лимит на количество хранимых examples и timestamps

### Надёжность
- [ ] **Обработка бинарных файлов** — детекция и корректный отказ
- [ ] **Обработка очень длинных строк** — truncate или skip
- [ ] **Graceful handling больших файлов** — progress reporting, возможность отмены

## 🟡 Важные улучшения

### Алгоритм кластеризации
- [ ] **Веса токенов** — числа менее значимы чем слова, IP адреса — ещё менее
- [ ] **Merge похожих кластеров** — после первичной кластеризации объединить близкие
- [ ] **Иерархическая кластеризация** — строить дерево сразу, а не при drill-down
- [ ] **Учёт позиции токена** — токены в начале строки важнее (уровень лога, timestamp)

### Временной анализ
- [ ] **Корреляция между кластерами** — показать что ERROR растёт когда падает throughput
- [ ] **Сезонность** — детекция периодических паттернов (каждый час, каждый день)
- [ ] **Trend detection** — рост/падение частоты со временем

### Новые инструменты
- [ ] **log_diff** — сравнить два временных периода (до/после деплоя)
- [ ] **log_anomaly** — автоматический поиск аномальных строк без кластеризации
- [ ] **log_context** — получить N строк до/после конкретной строки
- [ ] **log_session** — группировка по session_id/request_id если есть

### UX
- [ ] **Прогресс-бар** — для долгих операций
- [ ] **Подсказки** — "попробуйте log_cluster_drill для детализации"
- [ ] **Автоопределение типа лога** — nginx, postgres, docker, etc.

## 🟢 Nice to have

### Форматы
- [ ] **JSON logs** — парсинг и кластеризация по полям
- [ ] **Multiline logs** — stack traces, SQL queries
- [ ] **Structured logs** — logfmt, key=value

### Интеграции  
- [ ] **Чтение из stdin** — pipe логов напрямую
- [ ] **Чтение из URL** — S3, HTTP
- [ ] **Чтение из docker** — `docker logs container_id`
- [ ] **Watch mode** — следить за файлом в реальном времени

### Визуализация
- [ ] **Mermaid диаграммы** — timeline как Gantt chart
- [ ] **Экспорт в CSV** — результаты кластеризации
- [ ] **HTML отчёт** — standalone файл с результатами

### Оптимизации
- [ ] **Bloom filter** — быстрая проверка "может ли строка принадлежать кластеру"
- [ ] **Suffix array** — для быстрого поиска подстрок
- [ ] **Memory-mapped files** — для очень больших файлов
- [ ] **Worker threads** — параллельная обработка

## 📝 Технический долг

- [ ] Добавить юнит-тесты для clustering.js
- [ ] Добавить юнит-тесты для timestamps.js  
- [ ] Добавить интеграционные тесты MCP протокола
- [ ] TypeScript типизация
- [ ] ESLint конфигурация
- [ ] CI/CD pipeline

## 💡 Идеи для исследования

- **Использование LLM для именования кластеров** — вместо шаблона "ERROR .* Connection" дать имя "Database Connection Failures"
- **Semantic similarity** — использовать embeddings для кластеризации семантически похожих но синтаксически разных сообщений
- **Anomaly scoring** — каждой строке давать score "насколько она необычна"
- **Root cause analysis** — автоматически находить первопричину каскада ошибок

## Приоритеты для MVP

Для первой рабочей версии достаточно:
1. ✅ Базовая кластеризация
2. ✅ Временной анализ
3. ✅ MCP интерфейс
4. [ ] Streaming для больших файлов
5. [ ] Базовые тесты

После этого можно публиковать и собирать фидбек.
